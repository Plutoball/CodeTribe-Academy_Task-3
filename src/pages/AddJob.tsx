// This page will be a form to allow users to add a new job application.

function AddJob() {
    return (
        <div>
            <h2>Add New Job Application</h2>
            {/* Form components will go here */}
        </div>
    );
};

export default AddJob;